{
  "entities": {
    "Profile": {
      "title": "Business Profile",
      "description": "Public business profile data",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "title": { "type": "string" },
        "bio": { "type": "string" },
        "email": { "type": "string" },
        "phone": { "type": "string" }
      }
    },
    "Appointment": {
      "title": "Appointment",
      "description": "Booking request from client",
      "type": "object",
      "properties": {
        "customerName": { "type": "string" },
        "customerEmail": { "type": "string" },
        "date": { "type": "string" },
        "time": { "type": "string" },
        "profileId": { "type": "string" }
      },
      "required": ["customerName", "customerEmail", "date", "time", "profileId"]
    },
    "Inquiry": {
      "title": "Lead/Inquiry",
      "description": "Contact message from client",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string" },
        "message": { "type": "string" },
        "profileId": { "type": "string" }
      },
      "required": ["name", "email", "message", "profileId"]
    },
    "Follower": {
      "title": "Follower",
      "description": "User who follows a business profile",
      "type": "object",
      "properties": {
        "profileId": { "type": "string" },
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "email": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["profileId", "email"]
    },
    "Campaign": {
      "title": "Marketing Campaign",
      "description": "Broadcast message sent to followers",
      "type": "object",
      "properties": {
        "profileId": { "type": "string" },
        "message": { "type": "string" },
        "imageUrl": { "type": "string" },
        "type": { "type": "string", "enum": ["WhatsApp", "Push", "Email"] },
        "status": { "type": "string" },
        "sentAt": { "type": "string", "format": "date-time" }
      },
      "required": ["profileId", "message", "type"]
    },
    "ChatSession": {
      "title": "Chat Session",
      "description": "Conversation between a customer and a business",
      "type": "object",
      "properties": {
        "profileId": { "type": "string" },
        "customerName": { "type": "string" },
        "customerEmail": { "type": "string" },
        "status": { "type": "string", "enum": ["Active", "Queued", "Closed"] },
        "lastMessage": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["profileId", "customerName", "status"]
    },
    "ChatMessage": {
      "title": "Chat Message",
      "description": "Individual message in a chat session",
      "type": "object",
      "properties": {
        "sessionId": { "type": "string" },
        "senderType": { "type": "string", "enum": ["Customer", "Agent", "AI"] },
        "text": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["senderType", "text", "timestamp"]
    }
  },
  "firestore": {
    "/profiles/{uid}": {
      "schema": "Profile",
      "description": "Professional profiles"
    },
    "/appointments/{id}": {
      "schema": "Appointment",
      "description": "Client bookings"
    },
    "/leads/{id}": {
      "schema": "Inquiry",
      "description": "Contact inquiries"
    },
    "/followers/{id}": {
      "schema": "Follower",
      "description": "Business followers"
    },
    "/campaigns/{id}": {
      "schema": "Campaign",
      "description": "Marketing broadcasts"
    },
    "/chat_sessions/{id}": {
      "schema": "ChatSession",
      "description": "Active and past support chats"
    },
    "/chat_sessions/{id}/messages/{msgId}": {
      "schema": "ChatMessage",
      "description": "Live messages"
    }
  }
}
